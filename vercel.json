{
    "rewrites": [
        {
            "source": "/(.*)",
            "destination": "/api/$1"
        }
    ],
    "functions": {
        "api/[...all].ts": {
            "includeFiles": "{templates,static}/**"
        }
    }
}